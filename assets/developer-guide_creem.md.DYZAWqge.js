import{_ as t,o as a,c as d,a3 as s}from"./chunks/framework.BveOjhN0.js";const u=JSON.parse('{"title":"Creem Payment Gateway","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"Creem Payment Gateway | SaleOS Docs"}],["meta",{"property":"og:description","content":"Official documentation for the SaleOS SaaS Platform."}],["meta",{"property":"og:url","content":"https://docs.saleos.app/developer-guide/creem"}],["meta",{"property":"og:image","content":"https://docs.saleos.app/og-image.svg"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Creem Payment Gateway | SaleOS Docs"}],["meta",{"name":"twitter:description","content":"Official documentation for the SaleOS SaaS Platform."}],["meta",{"name":"twitter:image","content":"https://docs.saleos.app/og-image.svg"}],["link",{"rel":"canonical","href":"https://docs.saleos.app/developer-guide/creem"}]]},"headers":[],"relativePath":"developer-guide/creem.md","filePath":"developer-guide/creem.md","lastUpdated":1784475221000}'),i={name:"developer-guide/creem.md"};function o(n,e,r,c,l,p){return a(),d("div",null,[...e[0]||(e[0]=[s(`<h1 id="creem-payment-gateway" tabindex="-1">Creem Payment Gateway <a class="header-anchor" href="#creem-payment-gateway" aria-label="Permalink to &quot;Creem Payment Gateway&quot;">​</a></h1><p>Creem is a second payment provider (Merchant of Record) that plugs into the existing gateway-agnostic billing stack. Business logic stays in <code>BillingEngine</code> / <code>ModuleSubscriptionService</code>; Creem only translates between Creem’s REST API and <code>PaymentGatewayInterface</code>.</p><p>Stripe remains fully supported and unchanged. Drivers never share implementation.</p><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BillingEngine → GatewayManager → PaymentGatewayInterface</span></span>
<span class="line"><span>                                      ├── ManualGateway</span></span>
<span class="line"><span>                                      ├── StripeGateway</span></span>
<span class="line"><span>                                      └── CreemGateway  (+ CreemClient, CreemWebhookVerifier)</span></span></code></pre></div><p>Do <strong>not</strong> add <code>creem_*</code> columns. Creem customer ids live in provider-neutral <code>tenant_gateway_customers.customer_reference</code>. Product/price mapping uses <code>payment_gateway_module_prices.gateway_*_reference</code>.</p><h2 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h2><ol><li>Pull backend changes and run migrations:</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> db:seed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --class=Database\\\\Seeders\\\\Central\\\\PaymentGatewaySeeder</span></span></code></pre></div><ol start="2"><li><p>Configure credentials (env fallbacks <strong>or</strong> Central admin UI).</p></li><li><p>Map each billable module to a Creem Product ID under <strong>Billing → Payment Gateways → Product mapping</strong>.</p></li><li><p>Point Creem webhooks to <code>POST /webhooks/gateways/creem</code>.</p></li><li><p>Enable Creem and optionally set it as the default gateway (only one default).</p></li></ol><h2 id="environment-variables" tabindex="-1">Environment variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment variables&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Variable</th><th>Purpose</th></tr></thead><tbody><tr><td><code>CREEM_ENABLED</code></td><td>Seed row starts active when <code>true</code></td></tr><tr><td><code>CREEM_ENV</code></td><td><code>sandbox</code> | <code>live</code> (seeded <code>mode</code>)</td></tr><tr><td><code>CREEM_API_URL</code></td><td>Optional override (default test/live Creem hosts)</td></tr><tr><td><code>CREEM_SECRET_KEY</code></td><td>API key fallback (<code>x-api-key</code>)</td></tr><tr><td><code>CREEM_WEBHOOK_SECRET</code></td><td>HMAC secret for <code>creem-signature</code></td></tr><tr><td><code>CREEM_TIMEOUT</code> / <code>CREEM_CONNECT_TIMEOUT</code></td><td>HTTP timeouts</td></tr></tbody></table><p>Prefer storing secrets in encrypted <code>payment_gateways.config</code> via the admin UI (<code>secret_key</code>, <code>webhook_secret</code>). Env values are fallbacks when the row is empty.</p><p>Config file: <code>config/creem.php</code>.</p><h2 id="sandbox-setup" tabindex="-1">Sandbox setup <a class="header-anchor" href="#sandbox-setup" aria-label="Permalink to &quot;Sandbox setup&quot;">​</a></h2><ol><li>Create a Creem test account → <strong>Developers → API Keys</strong> (<code>ck_test_…</code>).</li><li>Create recurring products in the Creem dashboard; copy each <code>prod_…</code> id.</li><li>In Central admin: enable Creem, mode <strong>sandbox</strong>, paste Secret key + Webhook secret.</li><li>Product mapping: put <code>prod_…</code> in <strong>Product reference</strong> (Price reference may repeat the same id).</li><li>Register webhook URL (ngrok / Herd share):</li></ol><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://your-app.test/webhooks/gateways/creem</span></span></code></pre></div><p>Subscribe at least: <code>checkout.completed</code>, <code>subscription.active</code>, <code>subscription.paid</code>, <code>subscription.canceled</code>, <code>subscription.expired</code>, <code>subscription.past_due</code>, <code>subscription.update</code>, <code>subscription.trialing</code>, <code>subscription.paused</code>, <code>subscription.scheduled_cancel</code>, <code>refund.created</code>.</p><h2 id="production-setup" tabindex="-1">Production setup <a class="header-anchor" href="#production-setup" aria-label="Permalink to &quot;Production setup&quot;">​</a></h2><ol><li>Switch Creem dashboard to live; create live API key + webhook secret.</li><li>Admin: mode <strong>live</strong>, paste live credentials (do not mix test keys).</li><li>Webhook URL must be HTTPS public: <code>https://api.yourdomain.com/webhooks/gateways/creem</code>.</li><li>Remap products to live <code>prod_…</code> ids.</li><li>Set Creem as default only after a successful <strong>Test connection</strong> and a sandbox end-to-end purchase.</li></ol><h2 id="webhook-configuration" tabindex="-1">Webhook configuration <a class="header-anchor" href="#webhook-configuration" aria-label="Permalink to &quot;Webhook configuration&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Item</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>POST /webhooks/gateways/{code}</code> with <code>code=creem</code></td></tr><tr><td>Signature header</td><td><code>creem-signature</code></td></tr><tr><td>Algorithm</td><td>HMAC-SHA256 over <strong>raw</strong> body, hex digest</td></tr><tr><td>CSRF</td><td>Excepted (<code>webhooks/gateways/*</code>)</td></tr><tr><td>Idempotency</td><td><code>webhook_logs.provider_event_id</code> unique</td></tr></tbody></table><p>Invalid signatures → <code>400</code> and a failed webhook log (no payload retention of attacker bodies).</p><h2 id="supported-events-mapping" tabindex="-1">Supported events &amp; mapping <a class="header-anchor" href="#supported-events-mapping" aria-label="Permalink to &quot;Supported events &amp; mapping&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Creem event</th><th>Normalized <code>GatewayEvent</code></th><th>Engine effect</th></tr></thead><tbody><tr><td><code>checkout.completed</code></td><td><code>payment_succeeded</code></td><td>Settle payment + activate pending module</td></tr><tr><td><code>subscription.paid</code></td><td><code>payment_succeeded</code></td><td>Preferred activation / renewal signal</td></tr><tr><td><code>subscription.active</code></td><td><code>subscription_created</code></td><td>Link <code>provider_subscription_id</code></td></tr><tr><td><code>subscription.trialing</code></td><td><code>subscription_created</code></td><td>Link subscription</td></tr><tr><td><code>subscription.update</code></td><td><code>subscription_updated</code></td><td>Link / sync subscription id</td></tr><tr><td><code>subscription.paused</code></td><td><code>subscription_updated</code></td><td>Sync only</td></tr><tr><td><code>subscription.scheduled_cancel</code></td><td><code>subscription_updated</code></td><td>Sync only (access until period end)</td></tr><tr><td><code>subscription.canceled</code></td><td><code>subscription_cancelled</code></td><td>Cancel module + forget entitlements</td></tr><tr><td><code>subscription.expired</code></td><td><code>subscription_cancelled</code></td><td>Cancel module</td></tr><tr><td><code>subscription.past_due</code></td><td><code>payment_failed</code></td><td>Mark payment failed</td></tr><tr><td><code>refund.created</code></td><td><code>unsupported</code></td><td>Logged ignored (dashboard refunds)</td></tr></tbody></table><p>Invoice-ish fields are attached on <code>GatewayEvent.meta</code> (<code>invoice_reference</code>, currency, tax, period, paid_at) without provider-specific DB columns.</p><h2 id="differences-vs-stripe" tabindex="-1">Differences vs Stripe <a class="header-anchor" href="#differences-vs-stripe" aria-label="Permalink to &quot;Differences vs Stripe&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Topic</th><th>Stripe</th><th>Creem</th></tr></thead><tbody><tr><td>SDK</td><td>Cashier + Stripe PHP SDK</td><td>Laravel HTTP client (<code>CreemClient</code>)</td></tr><tr><td>Customer store</td><td><code>tenants.stripe_id</code></td><td><code>tenant_gateway_customers.customer_reference</code></td></tr><tr><td>Checkout price</td><td>Stripe Price <code>price_…</code></td><td>Creem Product <code>prod_…</code></td></tr><tr><td>Activation webhook</td><td><code>invoice.payment_succeeded</code></td><td><code>subscription.paid</code> / <code>checkout.completed</code></td></tr><tr><td>Dual ingress</td><td><code>/stripe/webhook</code> + generic</td><td>Generic only</td></tr><tr><td>Programmatic refunds</td><td>Stripe Refunds API</td><td>Dashboard + <code>refund.created</code> webhook</td></tr><tr><td>Customer portal</td><td>Cashier billing portal</td><td><code>POST /v1/customers/billing</code> → <code>createPortalSession()</code></td></tr></tbody></table><h2 id="checkout-return-sync-local-missed-webhooks" tabindex="-1">Checkout return sync (local / missed webhooks) <a class="header-anchor" href="#checkout-return-sync-local-missed-webhooks" aria-label="Permalink to &quot;Checkout return sync (local / missed webhooks)&quot;">​</a></h2><p>Creem redirects to <code>/marketplace?checkout=success&amp;module={slug}</code> and appends <code>checkout_id</code>. The SPA calls:</p><p><code>POST /api/tenant/v1/marketplace/modules/{id}/confirm-checkout</code></p><p>which retrieves the Creem checkout and activates the pending module when status is <code>completed</code>. <strong>Complete subscription</strong> also syncs existing checkout attempts before creating a new session — so a paid-but-pending module no longer opens a second Creem checkout.</p><p>Webhooks remain the primary production path; return sync is the safety net when the webhook cannot reach local/dev.</p><h2 id="local-development" tabindex="-1">Local development <a class="header-anchor" href="#local-development" aria-label="Permalink to &quot;Local development&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Optional env fallbacks</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CREEM_ENABLED</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CREEM_ENV</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sandbox</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CREEM_SECRET_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ck_test_…</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CREEM_WEBHOOK_SECRET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">…</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> db:seed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --class=Database\\\\Seeders\\\\Central\\\\PaymentGatewaySeeder</span></span></code></pre></div><p>Forward Creem sandbox webhooks with ngrok to <code>/webhooks/gateways/creem</code>.</p><p>Run tests (no network):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Central/Billing/CreemGatewayTest.php</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">php</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> artisan</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --compact</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/Feature/Central/Billing/CreemWebhookTest.php</span></span></code></pre></div><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Symptom</th><th>Check</th></tr></thead><tbody><tr><td>Checkout fails “mapping missing”</td><td>Product mapping <code>prod_…</code> for the default gateway</td></tr><tr><td>Mode mismatch</td><td>Sandbox mode with live keys (or reverse)</td></tr><tr><td>Signature failures</td><td>Raw body HMAC; header name <code>creem-signature</code>; webhook secret matches dashboard</td></tr><tr><td>Tenant not resolved</td><td>Metadata must include <code>tenant_id</code> / payment ids, or customer row in <code>tenant_gateway_customers</code></td></tr><tr><td>Module not activating</td><td>Wait for <code>subscription.paid</code> (not only <code>subscription.active</code>); confirm webhook log <code>processed</code></td></tr><tr><td>Portal fails</td><td>Customer must exist (<code>syncCustomer</code> / prior checkout)</td></tr></tbody></table><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="/developer-guide/payment-gateways-overview">Payment gateway architecture</a></li><li><a href="/developer-guide/payment-gateways">Developer guide</a></li><li><a href="/developer-guide/payment-gateways-webhooks">Webhooks</a></li><li><a href="/developer-guide/billing-engine">Billing Engine</a></li><li><a href="/developer-guide/stripe-cashier">Stripe / Cashier</a></li></ul>`,42)])])}const m=t(i,[["render",o]]);export{u as __pageData,m as default};
