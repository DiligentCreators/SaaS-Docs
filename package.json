{
  "name": "saleos-docs",
  "private": true,
  "version": "1.1.0",
  "description": "Official documentation for the SaleOS SaaS Platform.",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs && node scripts/ensure-clean-url-indexes.mjs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "engines": {
    "node": ">=20"
  }
}
